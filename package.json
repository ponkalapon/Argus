{
  "name": "argus",
  "version": "1.0.0",
  "description": "Argus AI Agent Platform",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm --workspaces run build",
    "dev": "npm --workspaces run dev",
    "install": "npm install --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
